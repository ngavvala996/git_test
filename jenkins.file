pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'pip install --upgrade pip'
                sh 'pip install pytest --force-reinstall'
                sh 'pytest tests.py'
            }
        }
    }
}
