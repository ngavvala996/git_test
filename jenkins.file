pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'python -m pip install pytest'
                sh 'pytest tests.py'
            }
        }
    }
}
